###.indexOf(array, value, [fromIndex=0])

Finds given value in an array and returns index of the position in that array. Only takes arrays as first argumet. If optional third argument [fromIndex] is not given the function will start looking for the value at the first positon. Otherwise it will start looking for value from postition [fromIndex].
